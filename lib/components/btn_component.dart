import 'package:flutter/material.dart';

class BtnComponent extends StatelessWidget {
  const BtnComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}